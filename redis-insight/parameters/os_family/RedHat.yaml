# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set values specific to:
#   salt['config.get']('os_family') == RedHat.
#
# You just need to add the key:values for this `os_family` that differ
# from `defaults.yaml` + `<osarch>.yaml`.
#
# If you do not need to provide defaults via the `os_family` config,
# you can remove this file or provide at least an empty dict, e.g.
# values: {}
---
values:
  pkg:
    name: 'redisinsight'
  config:
    app_dir: '/opt/Redis Insight'
    browser_policy_dir: '/etc/opt/chrome/policies/managed'
    desktop_file: '/usr/share/applications/redis-insight.desktop'
    eula_accepted: true
    global_cfg: '/etc/redis-insight.d/config.json'
    port: 5540
    profile_file: '/etc/profile.d/redis-insight.sh'
    system_group: 'root'
    telemetry: false
...
